# Reference material

Implementation must follow the Markdown/JSON contract at the package root first.

This folder contains:
- prior Product Definition, role matrix, and functional map artifacts for traceability;
- canonical user-journey Mermaid sources.

When a reference artifact conflicts with a confirmed decision in the current Codex specification, the current Codex specification controls for the disposable demo.
